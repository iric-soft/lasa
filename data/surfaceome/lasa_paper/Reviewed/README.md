Small documentation to generate these data

Maxquant output reformated to keep some columns, like:
 * Severals information: IDs, coverage, # peptides, ...
 * Intensity columns renamed as **Sample x Area**, with x as the **track** number of the sample available in surfaceome/lasa_paper/samples.tsv
